# Bash aliases

# General
alias sudo='sudo ' # Run alias with sudo
alias htop='htop -t'
alias ls='ls --hyperlink=auto --color=auto'
alias l='ls'
alias ll='ls -la'
alias vi='nvim'

# SSH
alias ssh='TERM=xterm-color ssh'
